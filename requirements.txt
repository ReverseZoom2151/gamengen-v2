# Legacy convenience file. pyproject.toml is the authoritative dependency map.
# Install core runtime dependencies with: pip install .
# Install optional game/metric integrations with: pip install '.[doom,dino,metrics]'
.
